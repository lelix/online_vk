= form_for @take do |f|
  - if @take.errors.any?
    #error_explanation
      h2 = "#{pluralize(@take.errors.count, "error")} prohibited this take from being saved:"
      ul
        - @take.errors.full_messages.each do |message|
          li = message

  .field
    = f.label :online
    = f.check_box :online
  .field
    = f.label :person
    = f.text_field :person
  .actions = f.submit
